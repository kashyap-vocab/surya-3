HF_TOKEN=your_huggingface_token_here

# Default to CPU (0). Change to 999 to offload all layers to GPU.
N_GPU_LAYERS=0

# Default to empty for CPU. Change to "--flash-attn on" if using a GPU.
FLASH_ATTN=